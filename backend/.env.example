# Django Configuration
SECRET_KEY=your-super-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (Choose one)

# Option 1: DATABASE_URL (for cloud deployments like Railway, Heroku)
# DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Option 2: Individual database settings
DB_NAME=social_media
DB_USER=postgres
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=5432

# For SQLite (development only)
# Leave all database settings empty to use SQLite

# Production Settings
# Set these for production deployment:
# DEBUG=False
# ALLOWED_HOSTS=your-domain.com,your-api.railway.app
# SECRET_KEY=your-actual-secret-key-from-generator